VITE_API_URL=/backend-api
VITE_CHAT_API_URL=/backend-api/api
# Socket.io can't be proxied effectively on Netlify, so it remains direct. The socket connection uses a token rather than strict CORS cookies.
VITE_SOCKET_URL=https://divtinder.onrender.com
VITE_BACKEND_TARGET=https://divtinder.onrender.com
